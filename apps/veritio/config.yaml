# Compatibility stub for direct `iii` invocations.
#
# Veritio's runtime topology lives in worker-compose.yaml and must be started
# with `iii compose --namespace default --up --file worker-compose.yaml`.
# iii 0.23 rejects project workers declared in this legacy file.
workers: []
